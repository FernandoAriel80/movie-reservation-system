{
    "build": {
        "commands": [
            "composer install --no-dev --optimize-autoloader"
        ]
    },
    "deploy": {
        "startCommand": "php-fpm -D && nginx -g 'daemon off;'"
      }
}