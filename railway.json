{
    "build": {
        "commands": [
            "composer install --no-dev --optimize-autoloader"
        ]
    },
    "deploy": {
        "startCommand": "php-fpm --fpm-config php-fpm.conf && nginx -g 'daemon off;'"
      }
}